class Income {
  String date, remarks, receipt, recievedBy, amount, balance;
  Income(
      {this.date,
      this.remarks,
      this.receipt,
      this.recievedBy,
      this.amount,
      this.balance});
}
